<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.leyou.item.mapper.SpecMapper">

    <select id="findGroupByCid" parameterType="long" resultType="com.leyou.item.pojo.Group">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
          This element was generated on Fri Aug 02 16:38:41 CST 2019.
        -->
        select * from tb_spec_group
        where cid = #{cid}
    </select>

    <update id="updateGroup" parameterType="com.leyou.item.pojo.Group">
      UPDATE tb_spec_group set NAME = #{name} where id = #{id}
    </update>

    <insert id="addGroup" parameterType="com.leyou.item.pojo.Group">
      INSERT  into tb_spec_group(name,cid) VALUES (#{name},#{cid})
    </insert>

</mapper>